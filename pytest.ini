[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    login_guest: marker for guest on login page
    login_user: marker for sign in user
    need_review: marker for review tests on Stepik